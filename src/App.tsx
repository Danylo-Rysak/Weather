import { AppContainer } from './app-styles/appStyles';
import { Header } from './components/Header/Header';

export const App = () => {
  return (
    <AppContainer>
      <Header />
    </AppContainer>
  );
};
